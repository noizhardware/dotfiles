# rename as "Makefile" for automatic detection
progname = 
includePath = 
libs = 
libpaths = 
$(progname).exe : $(progname).c
	gcc -g -Wall -Wno-comment -o $@ $^ -L $(libpaths) -l $(libs) -I $(includePath)