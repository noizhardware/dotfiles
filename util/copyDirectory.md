`sudo cp -r /path/to/source /path/to/dest`
copies the source folder into dest, creating `dest/source`

`sudo cp -r /path/to/source/. /path/to/dest`
copies the contents of source into dest directly

keywords;
copy directory folder terminal linux

created: 2026g06-2217
